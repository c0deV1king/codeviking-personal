---
import { blogs } from "../../data/blogs";
import BlogBlock from "../ui/BlogBlock.astro";
---

<div class="flex justify-center mt-1 mb-8">
  <div class="flex flex-col p-0 w-[100%] sm:w-[50%]">
    <p class="text-2xl font-bold italic self-center sm:self-center">things i want to talk about</p>
    <ul class="space-y-4 self-center">
      {
        blogs.map((blog) => (
          <BlogBlock
            title={blog.title}
            date={blog.date}
            slug={blog.slug}
          />
        ))
      }
    </ul>
  </div>
</div>
